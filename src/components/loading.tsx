const Loading = () => {
  return (
    <div className="flex items-center w-full h-full text-6xl animate-ping place-content-center text-primary-900">
      <p>Loading…</p>
    </div>
  );
};

export default Loading;
